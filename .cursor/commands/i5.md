---
description: Alias for /cycle — same full pipeline (cleanup → iterate ×5 → ship)
---

# /i5 — ModelRouter

**Alias for `/cycle`.** Do not run a shortened version.

Execute the **full global cycle** in `.cursor/commands/cycle.md` — all six phases:

0. Orient → 1. `/cleanup` → 2. `/deepclean` → 3. `/cleanship` baseline → 4. **five iterations** → 5. `/cleanship` final → 6. `/ship`

Reference: `docs/CYCLES.md`.
