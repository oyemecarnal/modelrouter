# Iteration 3.1 — Daemon reliability & health probes

**Version:** 2.5.1 (patch)  
**Focus:** Security warn, pid tracking, 0.0.0.0 health bind

## Changes

- `doctor.sh` — warn on placeholder `LITELLM_SALT_KEY`; reconcile pidfile from port listener
- `start.sh` — `exec litellm` without pipe (pidfile tracks real process)
- `healthcheck.sh` — probe via `127.0.0.1` when `MODELROUTER_HOST=0.0.0.0`
- Docs — LAN gateway URL alignment (`Kevins-Mac-mini.local`); `homelab-status` reads `hosts.yaml`

## Exit

- [x] `make test` + `make lint`
- [x] `make health` alive after restart
- [ ] tower SSH (deferred)
