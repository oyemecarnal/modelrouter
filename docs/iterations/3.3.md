# Iteration 3.3 — Deploy + mini parity

**Focus:** Roll Cycle 3 fixes to kc-mini; verify remote health

## Changes

- `make deploy-mini` — sync 3.x tree to kc-mini, restart daemon
- Mini retains `MODELROUTER_HOST=0.0.0.0` + healthcheck loopback probe

## Exit

- [x] mini `status: alive` after `make restart` (deploy may race with launchd — double-stop added)
- [x] remote-health ok for `Kevins-Mac-mini.local`
