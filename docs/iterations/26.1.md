# Iteration 26.1 — Security: OAuth state + exchange gate

- `modelrouter/oauth_exchange.py` — CSRF state in `~/Library/Application Support/ModelRouter/oauth`
- Real Google token exchange only when `OAUTH_EXCHANGE=1` + client credentials
- Refresh token stored via `env_store.update_env_file`; never returned in HTTP body
