# Iteration 1.2 — Network simplicity

**Version:** 1.2.0  
**Goal:** One config file for homelab hosts and LAN URLs.

## Changes

- `config/hosts.yaml` — laptop, kc-mini, kc-tower roles + client mapping
- `scripts/remote-health.sh` — laptop + mini + optional tower→mini
- `Makefile` target `remote-health`

## Test

```bash
make remote-health
```

## Result

- [x] `config/hosts.yaml` validates
- [x] mini `status: alive`
- [ ] tower — `KC_TOWER_SSH` not configured yet

Set `KC_TOWER_SSH` if alias isn't `kc-tower-lan`.
