# Iteration 25.1 — Security: vault export deny

- `config/key_vault.yaml` — `export_deny_vars`, `export_deny_prefixes` (`MODELROUTER_KEY_*`)
- `key_vault.export_blocked()` — filter before `export_env` writes `.env`
- `test.sh` — export deny assertions
