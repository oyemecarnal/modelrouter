# Iteration 5.2 — Doc drift

**Focus:** LAN HTTP URL consistency

## Changes

- `docs/ITERATION_REVIEW.md` — tower template uses `Kevins-Mac-mini.local`, not `kc-mini-lan` for HTTP

## Exit

- [x] Matches `config/client.env.example` and `hosts.yaml`
