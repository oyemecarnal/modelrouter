# Iteration 10.1 — Security: connector registry validation

- `test.sh` — `connectors.yaml` must not contain secret-like values (`gsk_`, `sk-ant-`)
- Required fields per connector: `env`, `validate`, `script`, `make_target`
- Script files must exist on disk
