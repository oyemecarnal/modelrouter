# Iteration 24.2 — Reliability: equity fetch timeout

- `fetch_usage._fetch_equity_safe` — 90s thread pool timeout (config: `equity.fetch_timeout_seconds`)
- `fetch_equity.read_stale_equity()` — return last cache on timeout so widget refresh completes
