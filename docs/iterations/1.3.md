# Iteration 1.3 — GitHub structure

**Version:** 1.3.0  
**Goal:** Clear repo layout doc + CI validates configs.

## Changes

- `docs/GITHUB.md`
- `.github/workflows/smoke.yml` — validate hosts.yaml + projects.yaml
- `CHANGELOG.md` started

## Test

```bash
make smoke
# CI equivalent runs on push
```
