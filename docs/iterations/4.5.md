# Iteration 4.5 — Catalog-driven max_tokens + ops → **v3.5.0**

**Version:** 3.5.0  
**Focus:** Correctness + operability — gateway caps match catalog SSOT

## Changes

- `scripts/sync_preset_max_tokens.py`, `make sync-preset-tokens` — primary routes get `max_tokens` from catalog
- `policy_presets.yaml`, `modelrouter.yaml`, `modelrouter.minimal.yaml` — synced caps
- `check_catalog.py` — fails if primary `max_tokens` drifts from catalog
- `doctor.sh` / `homelab-status.sh` — mention `make core-apis`

## Exit

- [x] 18 primary preset entries capped (6 presets × 3 configs)
- [x] `make sync-preset-tokens` idempotent
- [x] `make test` + `make lint`
