# Iteration 2.5 — Polish & committee fixes

**Version:** 2.5.0  
**Focus:** Bug fixes, widget security, cycle 2 capstone

## Changes

- Widget `X-Widget-Token` auth on mutating HTTP routes
- `route_policy` — `--all` no longer clobbered by single-project write
- `fetch_equity` — skip paper `status.json` when `force_live`
- `equity_remote_runner` — total includes all priced assets (up to 10 quotes)
- `api_assess` — preset list + offline IndexError fix
- MCP `list_models` — graceful error when gateway down
- `fetch_wallets` — redact full address from snapshot

## Exit

- [x] `make test` + `make lint`
- [x] `make homelab-status` laptop + mini green (tower SSH deferred)
