# Iteration 26.2 — Reliability: 429 → vault rotate hints

- `key_vault.record_rate_limit()` — round-robin advance when ≥2 keys for provider
- `logging_callback.async_log_failure_event` — detects 429, writes `data/key_rotate_hints.json`
- Doctor warns when hints file has recent rotates
