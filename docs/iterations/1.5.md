# Iteration 1.5 — Homelab integration

**Version:** 1.5.0  
**Goal:** One command for homelab visibility.

## Changes

- `scripts/homelab-status.sh` — doctor + remote-health + cost-review header
- `make homelab-status`
- `CHANGELOG.md` at 1.5.0
- `docs/ITERATION_REVIEW.md` — AI synthesis after cycle

## Test

```bash
make homelab-status
make test && make lint
```

## Cycle complete

Next cycle starts at 2.1 — pick one goal from `ITERATION_REVIEW.md` backlog.
