# Iteration 1.1 — Charter & test harness

**Version:** 1.1.0  
**Goal:** Establish homelab intent and a single `make test` entrypoint.

## Changes

- `VERSION` file
- `docs/HOMELAB_GOALS.md`
- `Makefile` target `test` (smoke + config validate)
- `docs/iterations/` index

## Test

```bash
make test
make restart   # if gateway down
make health
```

## Result

- [x] `make test` pass
- [x] health alive (after `make restart`)
