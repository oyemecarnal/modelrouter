# Iteration 2.1 — Clean wires

**Version target:** 2.1.0  
**Focus:** Auth bootstrap, tower client template, fail-closed startup

## Changes

- `scripts/check-master-key.sh` — refuse start on placeholder master key
- `scripts/issue-project-keys.sh --refresh` — sync `MODELROUTER_KEY_*` after rotate
- `rotate-master-key.sh` calls project-keys refresh
- `config/client.env.example` — no shell expansion; paste-from-mini instructions

## Exit

- [ ] `make rotate-master-key` run on mini (human)
- [ ] Tower uses `client.env` with gateway URL only
- [x] Fail-closed startup wired
