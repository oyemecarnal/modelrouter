# Iteration 4.2 — Core API list (private)

**Version:** 3.2.0  
**Focus:** Live masked API inventory, gitignored doc, catalog sync

## Changes

- `make core-apis` → `data/CORE_APIS.md` (gitignored, never committed)
- `modelrouter/core_api_list.py` — merges catalog, models, projects, laptop+mini status
- `config/api_catalog.yaml` — gateway project keys, Anthropic model ref
- `docs/API_INVENTORY.md` — pointer to private doc + commands

## Exit

- [x] `make core-apis` generates 240-line masked report
- [x] no full secrets in output
- [x] `make test` + `make lint`
