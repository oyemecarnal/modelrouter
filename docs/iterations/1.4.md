# Iteration 1.4 — Lint harness

**Version:** 1.4.0  
**Goal:** Fast local quality gate before deploy.

## Changes

- `scripts/lint.sh` — shellcheck/bash -n, python compileall, yaml parse
- `Makefile` target `lint`

## Test

```bash
make lint
make test
```
