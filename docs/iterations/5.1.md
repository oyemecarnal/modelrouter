# Iteration 5.1 — Test harness hardening

**Focus:** Security + correctness guards in `make test`

## Changes

- `scripts/test.sh` — `sync_preset_max_tokens` idempotency check
- `scripts/test.sh` — `preset_catalog` load smoke (≥6 policy presets)
- `scripts/test.sh` — assert `data/CORE_APIS.md` in `.gitignore`

## Exit

- [x] `make test` passes all new sections
- [x] No secrets in preset_catalog output path
