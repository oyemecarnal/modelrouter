# Iteration 26.3 — Correctness: widget vault toggle

- `vault_snapshot.py` — masked `list_entries` in widget snapshot
- `desktop_widget` `POST /vault/toggle` → `set_enabled`
- Widget Key vault section with enable/disable buttons
