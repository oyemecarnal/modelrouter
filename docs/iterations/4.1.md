# Iteration 4.1 — Phase 1 catalog SSOT

**Version:** 3.1.0  
**Focus:** `models_catalog.yaml` + drift check; master key decision

## Changes

- `config/models_catalog.yaml` — presets, models, cost tiers, context windows, max_tokens_default
- `scripts/check_catalog.py` + `make check-catalog` in `make test`
- `modelrouter/models_catalog.py` — loader for Console/widget
- `scripts/rotate-salt-key.sh` — rotate salt only; **master key unchanged** (`crsr_*` kept for Cursor)

## Master key decision

Keep existing `MODELROUTER_MASTER_KEY` (`crsr_*`). It passes fail-closed checks, works as gateway bearer, and avoids breaking Cursor. Use `make rotate-salt-key` for Docker/Postgres prep without rotating master.

## Exit

- [x] `make test` + `make lint`
- [x] catalog covers all policy presets
