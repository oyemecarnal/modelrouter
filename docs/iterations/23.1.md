# Iteration 23.1 — Portfolio equity + key vault + themes

## Shipped (v3.23.0)

- Network key vault: scrape homelab hosts → `data/key_vault.json` → export `.env` with `__ALT_N` keys
- Portfolio: Kraken (local coinbot), Coinbase (mini), Kalshi (live), Tangem watch-only
- Widget: 11 theme presets, Portfolio UI with per-source asset rows
- Tests: equity broker_routes, price_oracle, key_vault merge

## Human follow-ups

- Kraken: wait for API lockout clear; add `TANGEM_*` addresses to `.env` if not set
- `make vault-export` + `make push-env-mini` when ready to centralize keys on mini
