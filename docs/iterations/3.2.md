# Iteration 3.2 — Stop/restart reliability

**Focus:** Clean shutdown when pidfile is stale or port orphaned

## Changes

- `stop.sh` — kills pidfile process and frees port 3000 (SIGTERM then SIGKILL)
- `doctor.sh` — policy preset check no longer crashes when gateway is down

## Exit

- [x] `make restart` cycle leaves port free
- [x] `make doctor` with gateway down — no traceback
