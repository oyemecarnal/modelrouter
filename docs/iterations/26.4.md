# Iteration 26.4 — Ops: oauth callback + doctor rotate hints

- `oauth_callback_stub` generates dev state on startup
- Doctor reads `key_rotate_hints.json` when present
- `docs/KEY_VAULT.md` — 429 → export workflow
