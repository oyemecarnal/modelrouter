# Iteration 4.4 — Widget policy presets

**Version:** 3.4.0 (internal)  
**Focus:** Operability — visible preset tier + token limits in Keys widget

## Changes

- `tokens/widget/index.html` — **Policy presets** section (cost tier badge, max tokens, clients, project routing)

## Exit

- [x] Section renders from `snapshot.policyPresets`
- [x] Degrades gracefully on catalog load error
- [x] `make lint`
