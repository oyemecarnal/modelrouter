# Iteration 41.1 — Stale pidfile reliability

- `lib.sh` — `modelrouter_port_listener_pid`, `modelrouter_gateway_listening`
- `start-daemon.sh` — detect wrapper pid without port listener; stop stale before start
- `doctor.sh` — report stale wrapper vs missing listener clearly
