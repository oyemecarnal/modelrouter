# Iteration 27.3 — Correctness: rotate export + daemon fix

- `make vault-rotate-export` — apply last 429 rotate hint → `.env` merge
- `daemon-enable` / `daemon-disable` — `launchctl bootstrap` / `bootout` for modern macOS
