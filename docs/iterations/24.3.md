# Iteration 24.3 — Correctness: stale equity flag

- Timeout path sets `stale: true` + error message when serving cached portfolio
- Widget portfolio section shows cached data instead of blocking entire snapshot
