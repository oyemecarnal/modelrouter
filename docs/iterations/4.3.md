# Iteration 4.3 — Preset catalog in snapshot

**Version:** 3.3.0 (internal)  
**Focus:** Security — catalog-only preset grid; no `.env` in widget path

## Changes

- `tokens/scripts/preset_catalog.py` — reads `models_catalog.yaml` + `projects.yaml`
- `fetch_usage.py` — `policyPresets` on snapshot JSON (tier, max tokens, clients, project map)

## Exit

- [x] Loader returns 6 policy presets + projects
- [x] Snapshot field never includes raw API keys
- [x] `make test`
