# Iteration 41.3 — Mini auto-rotate armed

- `make enable-auto-rotate-mini --enable` — `MODELROUTER_AUTO_VAULT_ROTATE/RESTART=1` on mini
- `make vault-rotate-drill` — 429 simulate passes

**Human backlog:** paste Mistral `#2` → `MISTRAL_API_KEY__ALT_1` for 4/4 shuffle
